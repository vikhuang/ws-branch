[project]
name = "ws-branch"
version = "0.1.0"
description = "High-speed PNL computation for stock trading data"
requires-python = ">=3.12"
dependencies = [
    "polars>=1.0.0",
    "numpy>=2.0.0",
    "google-cloud-bigquery>=3.40.0",
    "xlsxwriter>=3.2.9",
]
