"""products:個股讀本(stock_readbook)與分點 profile(broker_profile),純渲染、零 IO。

labels / signals 未實作——Step F 盤點(docs/STEP_F_INVENTORY_2026-09-21.md)證無消費端,
待需求再立;不得以 cosine 門檻默默補成舊 retail/institutional 標籤(§10.2)。
"""
